worker_processes auto;

events {}

http {

    upstream client {
        server client:3000;
    }

    server {
        listen 80;
        server_name ${NGINX_DOMEN} www.${NGINX_DOMEN};
        server_tokens off;

        location / {
            proxy_pass http://client;
        }
    }
}
