FROM nginx:stable-alpine
COPY ./nginx/dev/nginx.conf.dev.template /etc/nginx/nginx.conf.dev.template

# Install envsubst
RUN apk add --no-cache gettext

# Substitute environment variables and run Nginx
CMD ["sh", "-c", "envsubst '${NGINX_DOMEN}' < /etc/nginx/nginx.conf.dev.template > /etc/nginx/nginx.conf && while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g 'daemon off;'"]