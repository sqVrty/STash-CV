version: '3'

services:
  nginx:
    container_name: nginx
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
      - 443:443
    env_file:
      - .env
    restart: always